const form=document.querySelector('#form'),message=document.querySelector('#message'),result=document.querySelector('#result'),shortUrl=document.querySelector('#shortUrl');
form.addEventListener('submit',async e=>{e.preventDefault();message.className='';message.textContent='Creating your short link...';result.hidden=true;try{const base=window.SNIPIFY_CONFIG?.API_BASE_URL;if(!base||base.includes('REPLACE'))throw new Error('Configure frontend/config.js with the deployed API URL.');const response=await fetch(`${base}/api/urls`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({originalUrl:document.querySelector('#url').value,expiryDays:Number(document.querySelector('#days').value)})});const data=await response.json();if(!response.ok)throw new Error(data.message||'Request failed');shortUrl.value=data.shortUrl;result.hidden=false;message.className='ok';message.textContent='Your Snipify link is ready.';}catch(error){message.textContent=error.message;}});
document.querySelector('#copy').addEventListener('click',async()=>{await navigator.clipboard.writeText(shortUrl.value);message.className='ok';message.textContent='Copied to clipboard.';});
